{
  "name": "codex-bible-study-app",
  "version": "0.0.0",
  "private": true,
  "description": "CODEX Bible-study PWA — dev tooling only. End users and deploy need ZERO install and ZERO build; the app ships as plain <script> tags with in-browser Babel.",
  "scripts": {
    "check": "node scripts/check.mjs"
  },
  "devDependencies": {
    "@babel/standalone": "7.29.0",
    "puppeteer-core": "^25.1.0"
  }
}
